'use client';

import Box from '@mui/material/Box';

const Loading = () => {
  return (
    <Box id="preloader">
      <div className="preloader-spin"></div>
    </Box>
  );
};

export default Loading;
